from setuptools import setup, find_packages

setup(
    name='package',
    version='0.1',
    packages=find_packages(),
    install_requires=[],
    author='Shelly S',
    author_email='blahblah@blah',
    description='just saying hello',
)